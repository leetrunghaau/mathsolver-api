// orderDTO.js

class OrderDTO {
    constructor(orderId, userId, createdAt,progress, discountId) {
      this.orderId = orderId,
      this.userId = userId,
      this.createdAt = createdAt,
      this.progress = progress,
      this.discountId = discountId
    }
  
    // Define DTO methods
  }
  
  module.exports = OrderDTO;
  