// userDTO.js

class UserDTO {
    constructor(userId, addressId, firstName, lastName, email, birthDate, gender, createdAt) {
      // Set DTO properties
      this.userId = userId,
      this.addressId = addressId,
      this.firstName = firstName,
      this.lastName = lastName,
      this.email = email,
      this.birthDate = birthDate,
      this.gender = gender,
      this, createdAt = createdAt
    }
  
    // Define DTO methods
  }
  
  module.exports = UserDTO;
  