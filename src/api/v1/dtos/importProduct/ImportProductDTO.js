// importDTO.js

class ImportDTO {
    constructor(importProductId, productId, distributerId, userId, quantity) {
      this.importProductId = importProductId,
      this.productId = productId,
      this.distributerId = distributerId,
      this.userId = userId,
      this.quantity = quantity
    }
  
    // Define DTO methods
  }
  
  module.exports = ImportDTO;
  